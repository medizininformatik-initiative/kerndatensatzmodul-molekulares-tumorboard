## Verification (generated — do not retype)

Produced by `verify-migration.py` from the target tree AND `migration-log/run.log`, the two oracles. **247 IDENTISCH · 36 DIVERGIERT · 148 NICHT PRÜFBAR.**

Verdicts: **IDENTISCH** = matches the source · **DIVERGIERT** = differs, named below · **NICHT PRÜFBAR** = could not be checked, which is **not** a pass and owes a named human an action.

| Layer | Check | What it asks | IDENTISCH | DIVERGIERT | NICHT PRÜFBAR |
|---|---|---|---|---|---|
| conservation | C1 | every source artefact still exists in the migrated module | 1 | 0 | 0 |
| conservation | C2 | every artefact is reachable from the rendered Artifacts page | 2 | 0 | 0 |
| conservation | C3 | every source guide page was migrated or explicitly retired | 102 | 0 | 0 |
| conservation | C4 | the source's narrative text is present somewhere in the target | 6 | 17 | 79 |
| conservation | C5 | menus lead somewhere, and every page is in a menu | 3 | 0 | 1 |
| conservation | C6 | each text passage landed on the page the page map promised | 75 | 0 | 0 |
| conservation | C7 | content the migration wrote is marked as such in the guide | 0 | 14 | 56 |
| fidelity | F1 | module identity is unchanged (id, canonical, version, licence, ...) | 9 | 0 | 0 |
| fidelity | F2 | dependency versions are pinned exactly as the source pinned them | 11 | 0 | 4 |
| fidelity | F3 | the licence is asserted from evidence, never defaulted | 2 | 0 | 0 |
| fidelity | F4 | no mechanical FSH conversion residue is left | 1 | 0 | 0 |
| provenance | P1 | the rendered site reports the template package it was built with | 1 | 0 | 0 |
| provenance | P2 | the module-template ref the skeleton came from is the latest release | 1 | 0 | 0 |
| provenance | P3 | the IG Publisher version matches the workflow pin | 1 | 0 | 1 |
| provenance | P4 | the source guide was pinned to a published version, not 'current' | 0 | 0 | 1 |
| provenance | P5 | ig.ini points at the IG resource the build actually writes | 1 | 0 | 0 |
| rendering | R1 | tables, tabs and images render with content, not empty | 2 | 0 | 1 |
| rendering | R2 | page header and footer metadata render correctly | 0 | 2 | 0 |
| rendering | R3 | a translated page really differs from the default language | 1 | 0 | 0 |
| rendering | R4 | no links point at template example artefacts that were deleted | 0 | 0 | 1 |
| rendering | R5 | every page has a title unit in the translation catalogue | 1 | 0 | 0 |
| log | L0 | a run log exists at all | 1 | 0 | 0 |
| log | L1 | every partial-success warning was acted on | 1 | 0 | 0 |
| log | L2 | every expected step actually wrote a log line | 23 | 0 | 2 |
| log | L3 | no identity contradiction is still open | 1 | 3 | 0 |
| log | L4 | the log's counts agree with what the tree holds | 1 | 0 | 2 |

### DIVERGIERT — each one a stop or a recorded decision

| id | Check | What it asks | Subject | Evidence | Next action | Auto-fixable |
|---|---|---|---|---|---|---|
| `C4-e4b03b` | C4 | the source's narrative text is present somewhere in the target | BeschreibungvonSzenarienfrdieAnwendungderModule.page.md | 24 of 92 PROSE runs of the source page are in no target page (first: ## Beschreibung von Szenarien für die Anwendung der Module t…) | map the missing text to a target page section, or record the loss in the report's content map | no |
| `C4-196ae3` | C4 | the source's narrative text is present somewhere in the target | CapabilityStatement.page.md | 3 of 6 PROSE runs of the source page are in no target page (first: Um eine dezentrale Datenauswertung mittel des Deutsche Forsc…) | map the missing text to a target page section, or record the loss in the report's content map | no |
| `C4-5e0695` | C4 | the source's narrative text is present somewhere in the target | DNPM-Transformation.page.md | 9 of 19 PROSE runs of the source page are in no target page (first: Diese Seite beschreibt die Transformation von [DNPM DIP]( MT…); 12 generated table row(s) excluded -- migration replaces that view with the artefact page (R1 checks THAT) | map the missing text to a target page section, or record the loss in the report's content map | no |
| `C4-22cb93` | C4 | the source's narrative text is present somewhere in the target | Datenstzeinkl.Beschreibungen.page.md | 1 of 5 PROSE runs of the source page are in no target page (first: Der Datensatz basiert auf dem Datensatz des [_Deutschen Netz…) | map the missing text to a target page section, or record the loss in the report's content map | no |
| `C4-fe322c` | C4 | the source's narrative text is present somewhere in the target | Genetische-Implikationen/Index.page.md | 5 of 9 PROSE runs of the source page are in no target page (first: fuer die Klassifikation eines Tumors oder die Bestaetigung e…) | map the missing text to a target page section, or record the loss in the report's content map | no |
| `C4-4489ed` | C4 | the source's narrative text is present somewhere in the target | In-Situ-Hybridization/Index.page.md | 1 of 3 PROSE runs of the source page are in no target page (first: * **FISH** (Fluorescence In Situ Hybridization) - Standard f…); 4 generated table row(s) excluded -- migration replaces that view with the artefact page (R1 checks THAT) | map the missing text to a target page section, or record the loss in the report's content map | no |
| `C4-0d15c2` | C4 | the source's narrative text is present somewhere in the target | Kommentierungsphase.page.md | 10 of 10 PROSE runs of the source page are in no target page (first: Diese Seite befasst sich mit der Kommentierungsphase und wir…) | map the missing text to a target page section, or record the loss in the report's content map | no |
| `C4-6b5222` | C4 | the source's narrative text is present somewhere in the target | KontextimGesamtprojektBezgezuanderenModulen.page.md | 4 of 20 PROSE runs of the source page are in no target page (first: Prozedur, Medikation und Diagnose - das Onkologie-Modul - de…) | map the missing text to a target page section, or record the loss in the report's content map | no |
| `C4-e07494` | C4 | the source's narrative text is present somewhere in the target | MIIIGModulMTB/Index.page.md | 10 of 14 PROSE runs of the source page are in no target page (first: Die vorliegende Spezifikation beschreibt die FHIR-Repräsenta…); 6 generated table row(s) excluded -- migration replaces that view with the artefact page (R1 checks THAT) | map the missing text to a target page section, or record the loss in the report's content map | no |
| `C4-b8a07a` | C4 | the source's narrative text is present somewhere in the target | Profile-Beziehungen-und-Referenzen.page.md | 3 of 3 PROSE runs of the source page are in no target page (first: Die folgende Übersicht soll die Referenzen der Ressourcen un…) | map the missing text to a target page section, or record the loss in the report's content map | no |
| `C4-db17c9` | C4 | the source's narrative text is present somewhere in the target | Profile-Inhalt-und-Vererbung.page.md | 1 of 2 PROSE runs of the source page are in no target page (first: Die Bilder können [hier (Github)]( einzeln betrachtet und he…) | map the missing text to a target page section, or record the loss in the report's content map | no |
| `C4-e5a7e3` | C4 | the source's narrative text is present somewhere in the target | QA-Validierung.page.md | 6 of 33 PROSE runs of the source page are in no target page (first: [`advisor.json`]( --- Diese Meldungen werden durch [`advisor…); 60 generated table row(s) excluded -- migration replaces that view with the artefact page (R1 checks THAT) | map the missing text to a target page section, or record the loss in the report's content map | no |
| `C4-fe91db` | C4 | the source's narrative text is present somewhere in the target | Referenzen.page.md | 2 of 3 PROSE runs of the source page are in no target page (first: ## Referenzen Das KDS-Modul _Molekulares Tumorboard_ basiert…) | map the missing text to a target page section, or record the loss in the report's content map | no |
| `C4-546e48` | C4 | the source's narrative text is present somewhere in the target | ReleaseNotes.page.md | 18 of 42 PROSE runs of the source page are in no target page (first: Update von 2025.0.4 auf 2026.0.0 (2026-Release) - `feat` **Q…) | map the missing text to a target page section, or record the loss in the report's content map | no |
| `C4-dcd614` | C4 | the source's narrative text is present somewhere in the target | Terminologien.page.md | 59 of 61 PROSE runs of the source page are in no target page (first: - [Definierte CodeSystems](#definierte-codesystems) - [Defin…) | map the missing text to a target page section, or record the loss in the report's content map | no |
| `C4-d47516` | C4 | the source's narrative text is present somewhere in the target | UML.page.md | 1 of 1 PROSE runs of the source page are in no target page (first: Die Bilddatei ist zur besseren Darstellung auch unter folgen…) | map the missing text to a target page section, or record the loss in the report's content map | no |
| `C4-ef59ff` | C4 | the source's narrative text is present somewhere in the target | index.md | 2 of 2 PROSE runs of the source page are in no target page (first: # MII Erweiterungsmodul Molekulares Tumorboard Willkommen be…) | map the missing text to a target page section, or record the loss in the report's content map | no |
| `C7-7437b8` | C7 | content the migration wrote is marked as such in the guide | anwendungsszenarien | 24 of 92 prose runs of the source page BeschreibungvonSzenarienfrdieAnwendungderModule.page.md are in no target page (C4), and anwendungsszenarien carries no DERIVED marker at all -- text was rewritten and the guide does not say so | mark the passage per spec section 9d (the DERIVED comment plus the visible box, in BOTH language mirrors), or restore the source wording; if the text was deliberately dropped, record that in migration-log/page-map.tsv instead | no |
| `C7-9191f0` | C7 | content the migration wrote is marked as such in the guide | capability-statements | 3 of 6 prose runs of the source page CapabilityStatement.page.md are in no target page (C4), and capability-statements carries no DERIVED marker at all -- text was rewritten and the guide does not say so | mark the passage per spec section 9d (the DERIVED comment plus the visible box, in BOTH language mirrors), or restore the source wording; if the text was deliberately dropped, record that in migration-log/page-map.tsv instead | no |
| `C7-cccbe2` | C7 | content the migration wrote is marked as such in the guide | dnpm-transformation | 9 of 19 prose runs of the source page DNPM-Transformation.page.md are in no target page (C4), and dnpm-transformation carries no DERIVED marker at all -- text was rewritten and the guide does not say so | mark the passage per spec section 9d (the DERIVED comment plus the visible box, in BOTH language mirrors), or restore the source wording; if the text was deliberately dropped, record that in migration-log/page-map.tsv instead | no |
| `C7-bd1fc5` | C7 | content the migration wrote is marked as such in the guide | logical-models | 1 of 5 prose runs of the source page Datenstzeinkl.Beschreibungen.page.md are in no target page (C4), and logical-models carries no DERIVED marker at all -- text was rewritten and the guide does not say so | mark the passage per spec section 9d (the DERIVED comment plus the visible box, in BOTH language mirrors), or restore the source wording; if the text was deliberately dropped, record that in migration-log/page-map.tsv instead | no |
| `C7-118c68` | C7 | content the migration wrote is marked as such in the guide | guidance | 4 of 20 prose runs of the source page KontextimGesamtprojektBezgezuanderenModulen.page.md are in no target page (C4), and guidance carries no DERIVED marker at all -- text was rewritten and the guide does not say so | mark the passage per spec section 9d (the DERIVED comment plus the visible box, in BOTH language mirrors), or restore the source wording; if the text was deliberately dropped, record that in migration-log/page-map.tsv instead | no |
| `C7-0209b1` | C7 | content the migration wrote is marked as such in the guide | index | 10 of 14 prose runs of the source page MIIIGModulMTB/Index.page.md are in no target page (C4), and index carries no DERIVED marker at all -- text was rewritten and the guide does not say so | mark the passage per spec section 9d (the DERIVED comment plus the visible box, in BOTH language mirrors), or restore the source wording; if the text was deliberately dropped, record that in migration-log/page-map.tsv instead | no |
| `C7-283e85` | C7 | content the migration wrote is marked as such in the guide | implementer-guidance | 3 of 3 prose runs of the source page Profile-Beziehungen-und-Referenzen.page.md are in no target page (C4), and implementer-guidance carries no DERIVED marker at all -- text was rewritten and the guide does not say so | mark the passage per spec section 9d (the DERIVED comment plus the visible box, in BOTH language mirrors), or restore the source wording; if the text was deliberately dropped, record that in migration-log/page-map.tsv instead | no |
| `C7-a62ca2` | C7 | content the migration wrote is marked as such in the guide | implementer-guidance | 1 of 2 prose runs of the source page Profile-Inhalt-und-Vererbung.page.md are in no target page (C4), and implementer-guidance carries no DERIVED marker at all -- text was rewritten and the guide does not say so | mark the passage per spec section 9d (the DERIVED comment plus the visible box, in BOTH language mirrors), or restore the source wording; if the text was deliberately dropped, record that in migration-log/page-map.tsv instead | no |
| `C7-1de5cd` | C7 | content the migration wrote is marked as such in the guide | implementer-guidance | 6 of 33 prose runs of the source page QA-Validierung.page.md are in no target page (C4), and implementer-guidance carries no DERIVED marker at all -- text was rewritten and the guide does not say so | mark the passage per spec section 9d (the DERIVED comment plus the visible box, in BOTH language mirrors), or restore the source wording; if the text was deliberately dropped, record that in migration-log/page-map.tsv instead | no |
| `C7-bab66a` | C7 | content the migration wrote is marked as such in the guide | guidance | 2 of 3 prose runs of the source page Referenzen.page.md are in no target page (C4), and guidance carries no DERIVED marker at all -- text was rewritten and the guide does not say so | mark the passage per spec section 9d (the DERIVED comment plus the visible box, in BOTH language mirrors), or restore the source wording; if the text was deliberately dropped, record that in migration-log/page-map.tsv instead | no |
| `C7-4f50d8` | C7 | content the migration wrote is marked as such in the guide | changes | 18 of 42 prose runs of the source page ReleaseNotes.page.md are in no target page (C4), and changes carries no DERIVED marker at all -- text was rewritten and the guide does not say so | mark the passage per spec section 9d (the DERIVED comment plus the visible box, in BOTH language mirrors), or restore the source wording; if the text was deliberately dropped, record that in migration-log/page-map.tsv instead | no |
| `C7-fabfa8` | C7 | content the migration wrote is marked as such in the guide | code-systems | 59 of 61 prose runs of the source page Terminologien.page.md are in no target page (C4), and code-systems carries no DERIVED marker at all -- text was rewritten and the guide does not say so | mark the passage per spec section 9d (the DERIVED comment plus the visible box, in BOTH language mirrors), or restore the source wording; if the text was deliberately dropped, record that in migration-log/page-map.tsv instead | no |
| `C7-8a1680` | C7 | content the migration wrote is marked as such in the guide | uml-diagrams | 1 of 1 prose runs of the source page UML.page.md are in no target page (C4), and uml-diagrams carries no DERIVED marker at all -- text was rewritten and the guide does not say so | mark the passage per spec section 9d (the DERIVED comment plus the visible box, in BOTH language mirrors), or restore the source wording; if the text was deliberately dropped, record that in migration-log/page-map.tsv instead | no |
| `C7-6c068a` | C7 | content the migration wrote is marked as such in the guide | index | 2 of 2 prose runs of the source page index.md are in no target page (C4), and index carries no DERIVED marker at all -- text was rewritten and the guide does not say so | mark the passage per spec section 9d (the DERIVED comment plus the visible box, in BOTH language mirrors), or restore the source wording; if the text was deliberately dropped, record that in migration-log/page-map.tsv instead | no |
| `R2-48ed52` | R2 | page header and footer metadata render correctly | output/de id="ig-status" [{{] | on 1 page(s), e.g. searchform.html: Search {{title}} (Current Build) | rendered header/footer metadata defect -- qa.txt does not report it. Fix the metadata it renders (a jurisdiction code the template cannot resolve is the measured case) | no |
| `R2-d1da9a` | R2 | page header and footer metadata render correctly | output/en id="ig-status" [{{] | on 1 page(s), e.g. searchform.html: Search {{title}} (Current Build) | rendered header/footer metadata defect -- qa.txt does not report it. Fix the metadata it renders (a jurisdiction code the template cannot resolve is the measured case) | no |
| `L3-1c7855` | L3 | no identity contradiction is still open | identity field canonical | 1 unresolved contradiction WARN(s), first at 2026-09-03T17:05:19Z: identity-contradiction: field=canonical now=https://www.medizininformatik-initiative.de/fhir/ext/modul-mtb (ti… | unresolved at verification time. It is a Gate-A decision, never a precedence puzzle to settle mechanically -- record it with a `decision:` line naming the field | no |
| `L3-f94875` | L3 | no identity contradiction is still open | identity field jurisdiction | 1 unresolved contradiction WARN(s), first at 2026-09-03T17:05:19Z: identity-contradiction: field=jurisdiction now=m49#276 (tier config, sushi-config.yaml) vs jurisdiction=http:/… | unresolved at verification time. It is a Gate-A decision, never a precedence puzzle to settle mechanically -- record it with a `decision:` line naming the field | no |
| `L3-fac1a0` | L3 | no identity contradiction is still open | identity field title | 1 unresolved contradiction WARN(s), first at 2026-09-03T17:05:19Z: identity-contradiction: field=title now=MII IG Kerndatensatz-Modul Molekulares Tumorboard (tier config, sushi-… | unresolved at verification time. It is a Gate-A decision, never a precedence puzzle to settle mechanically -- record it with a `decision:` line naming the field | no |

### NICHT PRÜFBAR — not a pass; each needs a human

| id | Check | What it asks | Subject | Why not mechanisable | Who does what |
|---|---|---|---|---|---|
| `C4-b56c53` | C4 | the source's narrative text is present somewhere in the target | Ablehnungsgrund-Extension.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 0 of 1 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-06bc24` | C4 | the source's narrative text is present somewhere in the target | Antrag-Kostenuebernahme-Claim/Index.page.md | the source page embeds an element-tree rendering (7 occurrences of the renderer's marker); 0 of 17 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-448148` | C4 | the source's narrative text is present somewhere in the target | Antrag-Kostenuebernahme/Index.page.md | the source page carries NO prose -- 0 generated table row(s) only | conservation of a generated view is not a text question; confirm the artefact page replaces it (R1) |
| `C4-24bf2e` | C4 | the source's narrative text is present somewhere in the target | Antragsstadium-Extension.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 0 of 1 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-42d75b` | C4 | the source's narrative text is present somewhere in the target | Antwort-Kostenuebernahme-ClaimResponse/Index.page.md | the source page embeds an element-tree rendering (7 occurrences of the renderer's marker); 2 of 17 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-7b2767` | C4 | the source's narrative text is present somewhere in the target | AnwendungsflleInformationsmodell/Index.page.md | the source page carries NO prose -- 0 generated table row(s) only | conservation of a generated view is not a text question; confirm the artefact page replaces it (R1) |
| `C4-b2d096` | C4 | the source's narrative text is present somewhere in the target | BRCAness-Observation.page.md | the source page embeds an element-tree rendering (7 occurrences of the renderer's marker); 0 of 12 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-e8f15a` | C4 | the source's narrative text is present somewhere in the target | Befund-Response-Observation.page.md | the source page embeds an element-tree rendering (7 occurrences of the renderer's marker); 4 of 21 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-f2bfa3` | C4 | the source's narrative text is present somewhere in the target | Befund-Response/Index.page.md | the source page carries NO prose -- 0 generated table row(s) only | conservation of a generated view is not a text question; confirm the artefact page replaces it (R1) |
| `C4-68ffe4` | C4 | the source's narrative text is present somewhere in the target | Behandlungsepisode-ClinicalImpression.page.md | the source page embeds an element-tree rendering (7 occurrences of the renderer's marker); 1 of 17 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-2ef10c` | C4 | the source's narrative text is present somewhere in the target | Behandlungsepisode/Index.page.md | the source page carries NO prose -- 0 generated table row(s) only | conservation of a generated view is not a text question; confirm the artefact page replaces it (R1) |
| `C4-c5cfa0` | C4 | the source's narrative text is present somewhere in the target | Biopsie-Auftrag-ServiceRequest.page.md | the source page embeds an element-tree rendering (7 occurrences of the renderer's marker); 1 of 14 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-f6c680` | C4 | the source's narrative text is present somewhere in the target | Consent-Observation.page.md | the source page embeds an element-tree rendering (7 occurrences of the renderer's marker); 0 of 11 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-c61fba` | C4 | the source's narrative text is present somewhere in the target | Copy-Number-Variant-Observation.page.md | the source page embeds an element-tree rendering (7 occurrences of the renderer's marker); 1 of 34 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-661fb0` | C4 | the source's narrative text is present somewhere in the target | DNA-Fusion-Observation.page.md | the source page embeds an element-tree rendering (7 occurrences of the renderer's marker); 0 of 38 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-0db42c` | C4 | the source's narrative text is present somewhere in the target | Diagnose-Primaertumor-Condition.page.md | the source page embeds an element-tree rendering (7 occurrences of the renderer's marker); 4 of 26 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-183b90` | C4 | the source's narrative text is present somewhere in the target | Diagnose/Index.page.md | the source page carries NO prose -- 0 generated table row(s) only | conservation of a generated view is not a text question; confirm the artefact page replaces it (R1) |
| `C4-e1b6af` | C4 | the source's narrative text is present somewhere in the target | Diagnostische-Implikation-Observation.page.md | the source page embeds an element-tree rendering (7 occurrences of the renderer's marker); 0 of 22 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-c76d16` | C4 | the source's narrative text is present somewhere in the target | Einfache-Variante-Observation.page.md | the source page embeds an element-tree rendering (7 occurrences of the renderer's marker); 0 of 32 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-95dd2a` | C4 | the source's narrative text is present somewhere in the target | Entscheidung-Extension.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 0 of 1 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-471749` | C4 | the source's narrative text is present somewhere in the target | Evidenzgraduierung-Extension.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 0 of 4 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-b88ce3` | C4 | the source's narrative text is present somewhere in the target | Extensions/Index.page.md | the source page carries NO prose -- 0 generated table row(s) only | conservation of a generated view is not a text question; confirm the artefact page replaces it (R1) |
| `C4-39c9c7` | C4 | the source's narrative text is present somewhere in the target | FHIRProfile/Index.page.md | the source page carries NO prose -- 0 generated table row(s) only | conservation of a generated view is not a text question; confirm the artefact page replaces it (R1) |
| `C4-1a2205` | C4 | the source's narrative text is present somewhere in the target | Follow-Up-ClinicalImpression.page.md | the source page embeds an element-tree rendering (7 occurrences of the renderer's marker); 2 of 12 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-9e05ab` | C4 | the source's narrative text is present somewhere in the target | Follow-Up/Index.page.md | the source page carries NO prose -- 0 generated table row(s) only | conservation of a generated view is not a text question; confirm the artefact page replaces it (R1) |
| `C4-5e8f5d` | C4 | the source's narrative text is present somewhere in the target | Genomic-Study-Device-Device.page.md | the source page embeds an element-tree rendering (11 occurrences of the renderer's marker); 0 of 11 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-fad786` | C4 | the source's narrative text is present somewhere in the target | Genomic-Study-Procedure.page.md | the source page embeds an element-tree rendering (7 occurrences of the renderer's marker); 0 of 15 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-f4a093` | C4 | the source's narrative text is present somewhere in the target | Genomic-Study/Index.page.md | the source page carries NO prose -- 0 generated table row(s) only | conservation of a generated view is not a text question; confirm the artefact page replaces it (R1) |
| `C4-745e91` | C4 | the source's narrative text is present somewhere in the target | Genomics-Study-Analysis-Procedure.page.md | the source page embeds an element-tree rendering (7 occurrences of the renderer's marker); 1 of 19 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-583f64` | C4 | the source's narrative text is present somewhere in the target | HRD-Score-Observation.page.md | the source page embeds an element-tree rendering (7 occurrences of the renderer's marker); 0 of 14 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-150017` | C4 | the source's narrative text is present somewhere in the target | Histologie-Evaluation-Auftrag-ServiceRequest.page.md | the source page embeds an element-tree rendering (7 occurrences of the renderer's marker); 1 of 12 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-e578c2` | C4 | the source's narrative text is present somewhere in the target | Human-genetische-Beratung-Auftrag-ServiceRequest.page.md | the source page embeds an element-tree rendering (7 occurrences of the renderer's marker); 1 of 13 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-6d726b` | C4 | the source's narrative text is present somewhere in the target | Immunhistochemie-HER2-Observation.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 0 of 11 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-017166` | C4 | the source's narrative text is present somewhere in the target | Immunhistochemie-MMR-MSI-Observation.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 0 of 17 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-e33ef6` | C4 | the source's narrative text is present somewhere in the target | Immunhistochemie-Observation.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 1 of 30 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-c98011` | C4 | the source's narrative text is present somewhere in the target | Immunhistochemie-PDL1-Observation.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 0 of 26 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-31f698` | C4 | the source's narrative text is present somewhere in the target | Immunhistochemie/Index.page.md | the source page carries NO prose -- 0 generated table row(s) only | conservation of a generated view is not a text question; confirm the artefact page replaces it (R1) |
| `C4-eea339` | C4 | the source's narrative text is present somewhere in the target | In-Situ-Hybridization-Observation.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 3 of 25 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-a29bc6` | C4 | the source's narrative text is present somewhere in the target | Kombinationstherapie-Empfehlung-RequestGroup.page.md | the source page embeds an element-tree rendering (15 occurrences of the renderer's marker); 3 of 12 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-987d3c` | C4 | the source's narrative text is present somewhere in the target | Leitlinienbehandlung-Status-Extension.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 0 of 4 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-c9a79f` | C4 | the source's narrative text is present somewhere in the target | Leitliniendokumentation-Extension.page.md | the source page embeds an element-tree rendering (15 occurrences of the renderer's marker); 0 of 5 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-093b4a` | C4 | the source's narrative text is present somewhere in the target | MIIIGModulMTB/TechnischeImplementierung/FHIRProfile/Behandlungsepisode/Therapieplan-MTB-Beschluss/Therapieempfehlung/Extensions/Index.page.md | the source page carries NO prose -- 0 generated table row(s) only | conservation of a generated view is not a text question; confirm the artefact page replaces it (R1) |
| `C4-da8580` | C4 | the source's narrative text is present somewhere in the target | Mikrosatelliteninstabilitaet-Observation.page.md | the source page embeds an element-tree rendering (7 occurrences of the renderer's marker); 0 of 28 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-8bf2e4` | C4 | the source's narrative text is present somewhere in the target | Molekular-Pathologie-Befund-DiagnosticReport.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 0 of 9 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-00b1f2` | C4 | the source's narrative text is present somewhere in the target | Molekular-Pathologie-Befund/Index.page.md | the source page carries NO prose -- 0 generated table row(s) only | conservation of a generated view is not a text question; confirm the artefact page replaces it (R1) |
| `C4-d39883` | C4 | the source's narrative text is present somewhere in the target | Molekularer-Biomarker-Observation.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 0 of 9 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-7c8147` | C4 | the source's narrative text is present somewhere in the target | Molekularer-Biomarker/Index.page.md | the source page carries NO prose -- 0 generated table row(s) only | conservation of a generated view is not a text question; confirm the artefact page replaces it (R1) |
| `C4-796a55` | C4 | the source's narrative text is present somewhere in the target | Mutationslast-Observation.page.md | the source page embeds an element-tree rendering (7 occurrences of the renderer's marker); 0 of 23 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-944368` | C4 | the source's narrative text is present somewhere in the target | NGS-Bericht-DiagnosticReport.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 0 of 15 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-685eba` | C4 | the source's narrative text is present somewhere in the target | NGS-Bericht/Index.page.md | the source page carries NO prose -- 0 generated table row(s) only | conservation of a generated view is not a text question; confirm the artefact page replaces it (R1) |
| `C4-963f4d` | C4 | the source's narrative text is present somewhere in the target | Oncotree-Observation.page.md | the source page embeds an element-tree rendering (7 occurrences of the renderer's marker); 1 of 12 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-6598ec` | C4 | the source's narrative text is present somewhere in the target | P-Immunhistochemie-Observation.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 1 of 23 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-a24b80` | C4 | the source's narrative text is present somewhere in the target | Ploidie-Observation.page.md | the source page embeds an element-tree rendering (7 occurrences of the renderer's marker); 0 of 10 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-a53377` | C4 | the source's narrative text is present somewhere in the target | Prioritaet-Extension.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 0 of 1 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-a31c90` | C4 | the source's narrative text is present somewhere in the target | Probe/Index.page.md | the source page carries NO prose -- 0 generated table row(s) only | conservation of a generated view is not a text question; confirm the artefact page replaces it (R1) |
| `C4-ba0d23` | C4 | the source's narrative text is present somewhere in the target | Publikation-Extension.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 0 of 2 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-893da3` | C4 | the source's narrative text is present somewhere in the target | RNA-Fusion-Observation.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 1 of 38 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-a21f04` | C4 | the source's narrative text is present somewhere in the target | RNA-Seq-Observation.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 1 of 33 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-35b633` | C4 | the source's narrative text is present somewhere in the target | RNA-Seq/Index.page.md | the source page carries NO prose -- 0 generated table row(s) only | conservation of a generated view is not a text question; confirm the artefact page replaces it (R1) |
| `C4-21def2` | C4 | the source's narrative text is present somewhere in the target | Studieneinschluss-Anfrage-ServiceRequest.page.md | the source page embeds an element-tree rendering (7 occurrences of the renderer's marker); 3 of 21 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-ac6515` | C4 | the source's narrative text is present somewhere in the target | Studieneinschluss-Studie-ResearchStudy.page.md | the source page embeds an element-tree rendering (7 occurrences of the renderer's marker); 18 of 41 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-e154ee` | C4 | the source's narrative text is present somewhere in the target | Studieneinschlussempfehlung/Index.page.md | the source page carries NO prose -- 0 generated table row(s) only | conservation of a generated view is not a text question; confirm the artefact page replaces it (R1) |
| `C4-f31311` | C4 | the source's narrative text is present somewhere in the target | Systemische-MTB-Therapie-Procedure.page.md | the source page embeds an element-tree rendering (7 occurrences of the renderer's marker); 1 of 20 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-8e677d` | C4 | the source's narrative text is present somewhere in the target | Systemische-Therapie-Medication-Statement-Medication-Statement.page.md | the source page embeds an element-tree rendering (7 occurrences of the renderer's marker); 0 of 14 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-daeb61` | C4 | the source's narrative text is present somewhere in the target | Systemische-Therapie/Index.page.md | the source page carries NO prose -- 0 generated table row(s) only | conservation of a generated view is not a text question; confirm the artefact page replaces it (R1) |
| `C4-2e0222` | C4 | the source's narrative text is present somewhere in the target | Systemische-Vortherapie-Procedure.page.md | the source page embeds an element-tree rendering (7 occurrences of the renderer's marker); 1 of 28 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-fc1144` | C4 | the source's narrative text is present somewhere in the target | TechnischeImplementierung/Index.page.md | the source page carries NO prose -- 0 generated table row(s) only | conservation of a generated view is not a text question; confirm the artefact page replaces it (R1) |
| `C4-163969` | C4 | the source's narrative text is present somewhere in the target | Therapeutische-Implikation-Observation.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 0 of 26 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-49889e` | C4 | the source's narrative text is present somewhere in the target | Therapieempfehlung-MedicationRequest.page.md | the source page embeds an element-tree rendering (7 occurrences of the renderer's marker); 1 of 28 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-9d9e17` | C4 | the source's narrative text is present somewhere in the target | Therapieempfehlung/Index.page.md | the source page carries NO prose -- 0 generated table row(s) only | conservation of a generated view is not a text question; confirm the artefact page replaces it (R1) |
| `C4-494197` | C4 | the source's narrative text is present somewhere in the target | Therapielinie-Extension.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 0 of 2 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-c628b2` | C4 | the source's narrative text is present somewhere in the target | Therapieplan-CarePlan.page.md | the source page embeds an element-tree rendering (7 occurrences of the renderer's marker); 2 of 26 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-a2d229` | C4 | the source's narrative text is present somewhere in the target | Therapieplan-MTB-Beschluss/Index.page.md | the source page carries NO prose -- 0 generated table row(s) only | conservation of a generated view is not a text question; confirm the artefact page replaces it (R1) |
| `C4-56ce72` | C4 | the source's narrative text is present somewhere in the target | Tumorausbreitung-Observation.page.md | the source page embeds an element-tree rendering (7 occurrences of the renderer's marker); 0 of 12 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-27f621` | C4 | the source's narrative text is present somewhere in the target | Tumorzellgehalt-Observation.page.md | the source page embeds an element-tree rendering (7 occurrences of the renderer's marker); 0 of 23 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-85c085` | C4 | the source's narrative text is present somewhere in the target | Variante/Index.page.md | the source page carries NO prose -- 0 generated table row(s) only | conservation of a generated view is not a text question; confirm the artefact page replaces it (R1) |
| `C4-5c7f4e` | C4 | the source's narrative text is present somewhere in the target | WHO-Grad-ZNS-Observation.page.md | the source page embeds an element-tree rendering (7 occurrences of the renderer's marker); 0 of 11 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C4-492487` | C4 | the source's narrative text is present somewhere in the target | Weitere-Empfehlungen/Index.page.md | the source page carries NO prose -- 0 generated table row(s) only | conservation of a generated view is not a text question; confirm the artefact page replaces it (R1) |
| `C4-78dad0` | C4 | the source's narrative text is present somewhere in the target | Zulassungsstatus-Extension.page.md | the source page embeds an element-tree rendering (5 occurrences of the renderer's marker); 0 of 2 runs are in no target page, but prose and rendered view cannot be told apart in it | read the page against its target: migration replaces the VIEW with the artefact page (R1), so only its prose has to be conserved -- and only a human can say which is which here |
| `C5-2d86b5` | C5 | menus lead somewhere, and every page is in a menu | target pages without a source counterpart | references/template-pages.tsv is measured at module-template v0.11.1, but this module vendors v0.13.2 -- a stale manifest produces confident wrong findings in both directions | re-measure template-pages.tsv at the vendored tag (record the tag in the third column), then re-run |
| `C7-22cca2` | C7 | content the migration wrote is marked as such in the guide | Ablehnungsgrund-Extension.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-f8a525` | C7 | content the migration wrote is marked as such in the guide | Antrag-Kostenuebernahme-Claim/Index.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-04eec0` | C7 | content the migration wrote is marked as such in the guide | Antragsstadium-Extension.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-6af7f5` | C7 | content the migration wrote is marked as such in the guide | Antwort-Kostenuebernahme-ClaimResponse/Index.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-bf52c6` | C7 | content the migration wrote is marked as such in the guide | BRCAness-Observation.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-fe5360` | C7 | content the migration wrote is marked as such in the guide | Befund-Response-Observation.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-323c8a` | C7 | content the migration wrote is marked as such in the guide | Behandlungsepisode-ClinicalImpression.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-34e8d2` | C7 | content the migration wrote is marked as such in the guide | Biopsie-Auftrag-ServiceRequest.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-204e33` | C7 | content the migration wrote is marked as such in the guide | Consent-Observation.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-236ab4` | C7 | content the migration wrote is marked as such in the guide | Copy-Number-Variant-Observation.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-fab779` | C7 | content the migration wrote is marked as such in the guide | DNA-Fusion-Observation.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-a5c02d` | C7 | content the migration wrote is marked as such in the guide | Diagnose-Primaertumor-Condition.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-f5dc90` | C7 | content the migration wrote is marked as such in the guide | Diagnostische-Implikation-Observation.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-c74730` | C7 | content the migration wrote is marked as such in the guide | Einfache-Variante-Observation.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-97003e` | C7 | content the migration wrote is marked as such in the guide | Entscheidung-Extension.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-158a4b` | C7 | content the migration wrote is marked as such in the guide | Evidenzgraduierung-Extension.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-8462e3` | C7 | content the migration wrote is marked as such in the guide | Follow-Up-ClinicalImpression.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-590ce2` | C7 | content the migration wrote is marked as such in the guide | Genomic-Study-Device-Device.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-48b81a` | C7 | content the migration wrote is marked as such in the guide | Genomic-Study-Procedure.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-518321` | C7 | content the migration wrote is marked as such in the guide | Genomics-Study-Analysis-Procedure.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-d713d7` | C7 | content the migration wrote is marked as such in the guide | HRD-Score-Observation.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-dcbda6` | C7 | content the migration wrote is marked as such in the guide | Histologie-Evaluation-Auftrag-ServiceRequest.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-18a78a` | C7 | content the migration wrote is marked as such in the guide | Human-genetische-Beratung-Auftrag-ServiceRequest.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-01be55` | C7 | content the migration wrote is marked as such in the guide | Immunhistochemie-HER2-Observation.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-92ab50` | C7 | content the migration wrote is marked as such in the guide | Immunhistochemie-MMR-MSI-Observation.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-ed2bac` | C7 | content the migration wrote is marked as such in the guide | Immunhistochemie-Observation.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-1c3cdf` | C7 | content the migration wrote is marked as such in the guide | Immunhistochemie-PDL1-Observation.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-0c06b6` | C7 | content the migration wrote is marked as such in the guide | In-Situ-Hybridization-Observation.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-f7c701` | C7 | content the migration wrote is marked as such in the guide | Kombinationstherapie-Empfehlung-RequestGroup.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-34103e` | C7 | content the migration wrote is marked as such in the guide | Leitlinienbehandlung-Status-Extension.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-a9200d` | C7 | content the migration wrote is marked as such in the guide | Leitliniendokumentation-Extension.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-b2affc` | C7 | content the migration wrote is marked as such in the guide | Mikrosatelliteninstabilitaet-Observation.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-a5871d` | C7 | content the migration wrote is marked as such in the guide | Molekular-Pathologie-Befund-DiagnosticReport.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-11ff81` | C7 | content the migration wrote is marked as such in the guide | Molekularer-Biomarker-Observation.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-c293b4` | C7 | content the migration wrote is marked as such in the guide | Mutationslast-Observation.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-fae832` | C7 | content the migration wrote is marked as such in the guide | NGS-Bericht-DiagnosticReport.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-dba516` | C7 | content the migration wrote is marked as such in the guide | Oncotree-Observation.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-30c511` | C7 | content the migration wrote is marked as such in the guide | P-Immunhistochemie-Observation.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-d8bbbf` | C7 | content the migration wrote is marked as such in the guide | Ploidie-Observation.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-6e2637` | C7 | content the migration wrote is marked as such in the guide | Prioritaet-Extension.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-ca37d8` | C7 | content the migration wrote is marked as such in the guide | Publikation-Extension.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-6a47c2` | C7 | content the migration wrote is marked as such in the guide | RNA-Fusion-Observation.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-15221b` | C7 | content the migration wrote is marked as such in the guide | RNA-Seq-Observation.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-a8b1b9` | C7 | content the migration wrote is marked as such in the guide | Studieneinschluss-Anfrage-ServiceRequest.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-82159e` | C7 | content the migration wrote is marked as such in the guide | Studieneinschluss-Studie-ResearchStudy.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-f6e757` | C7 | content the migration wrote is marked as such in the guide | Systemische-MTB-Therapie-Procedure.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-a67a04` | C7 | content the migration wrote is marked as such in the guide | Systemische-Therapie-Medication-Statement-Medication-Statement.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-c4cb8b` | C7 | content the migration wrote is marked as such in the guide | Systemische-Vortherapie-Procedure.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-0b32bd` | C7 | content the migration wrote is marked as such in the guide | Therapeutische-Implikation-Observation.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-4b8a1b` | C7 | content the migration wrote is marked as such in the guide | Therapieempfehlung-MedicationRequest.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-d359ca` | C7 | content the migration wrote is marked as such in the guide | Therapielinie-Extension.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-3f19a3` | C7 | content the migration wrote is marked as such in the guide | Therapieplan-CarePlan.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-74d042` | C7 | content the migration wrote is marked as such in the guide | Tumorausbreitung-Observation.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-98d720` | C7 | content the migration wrote is marked as such in the guide | Tumorzellgehalt-Observation.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-4b713f` | C7 | content the migration wrote is marked as such in the guide | WHO-Grad-ZNS-Observation.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `C7-2ac354` | C7 | content the migration wrote is marked as such in the guide | Zulassungsstatus-Extension.page.md | C4 could not tell this page's prose from its embedded element-tree rendering, so whether anything was rewritten here is not measured | read the page against its target (the same human action C4 asks for) and mark whatever the migration wrote per spec section 9d |
| `F2-fd0e73` | F2 | dependency versions are pinned exactly as the source pinned them | hl7.fhir.uv.crmi | target-only dependency 2.0.0 (not in the source) | confirm at Gate A that this is template machinery (hl7.fhir.uv.crmi is) and not an accidental addition |
| `F2-047205` | F2 | dependency versions are pinned exactly as the source pinned them | hl7.fhir.uv.extensions.r4 | target-only dependency 5.3.0 (not in the source) | confirm at Gate A that this is template machinery (hl7.fhir.uv.crmi is) and not an accidental addition |
| `F2-e78ce4` | F2 | dependency versions are pinned exactly as the source pinned them | hl7.fhir.uv.xver-r5.r4 | target-only dependency 0.1.0 (not in the source) | confirm at Gate A that this is template machinery (hl7.fhir.uv.crmi is) and not an accidental addition |
| `F2-8f9355` | F2 | dependency versions are pinned exactly as the source pinned them | hl7.terminology.r4 | target-only dependency 7.3.0 (not in the source) | confirm at Gate A that this is template machinery (hl7.fhir.uv.crmi is) and not an accidental addition |
| `P3-c39a79` | P3 | the IG Publisher version matches the workflow pin | IG Publisher currency | the publisher reports itself out of date (current 2.3.3) | upgrading the publisher is a target-repository decision, not a migration one -- record it, do not act on it here |
| `P4-379bde` | P4 | the source guide was pinned to a published version, not 'current' | source guide version | no `?version=` recorded in the run log or the harvest manifest | record the pinned, PUBLISHED guide version like the source commit SHA (spec 5.1c.3) |
| `R1-76f413` | R1 | tables, tabs and images render with content, not empty | source-versus-target rendering | no harvested source HTML (./migration-log/guide-harvest/html) and/or no page map | harvest with --keep-html and write the page map; without a source rendering, 'non-empty where non-empty in the source' has no reference |
| `R4-84ad62` | R4 | no links point at template example artefacts that were deleted | template example links | references/template-artifacts.tsv is verified at module-template v0.11.1, but this module vendors v0.13.2 -- a stale token list looks for the wrong scaffolding in both directions | re-verify the token list at the vendored tag (update the `# template_tag:` header), then re-run |
| `L2-7163a2` | L2 | every expected step actually wrote a log line | 2.1 repo-identity | no line in the log; the step is conditional (no sushi-config.yaml in the source) | confirm the condition did not hold -- The repository's LICENSE text is the only machine source for `license` — the field that must never default (spec §2.2). |
| `L2-930ba2` | L2 | every expected step actually wrote a log line | 5.1d guide-harvest | no line in the log; the step is conditional (the narrative is not in the repository) | confirm the condition did not hold -- This is the step whose absence shipped the template's starter pages under a module's name. |
| `L4-106e61` | L4 | the log's counts agree with what the tree holds | conversion count | no `gofsh-convert … actual=` line in the log | shape B only; for shape A there is nothing to convert |
| `L4-f17740` | L4 | the log's counts agree with what the tree holds | page count | no harvested count in the log and/or no harvest manifest | harvest the guide (step 2c) where the narrative is not in the repo |

**Inputs:** target `.` · source `/Users/thome/code/fhir-profiling/tools/migration-mtb/source-dev` · rendered `/Users/thome/code/fhir-profiling/mtb-template-migration/output` · log `./migration-log/run.log`
